{
  "version": 2,
  "builds": [{ "src": "*.js", "use": "@vercel/node" }],
    "routes": [
      { "src": "/(.*)", "dest": "/" },
      { "src": "^/login/([a-zA-Z0-9_-]+)$", "dest": "/login" },
      { "src": "/(.*)", "dest": "/dash" }

    ]
}
