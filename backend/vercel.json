{
  "version": 2,
  "builds": [{ "src": "*.js", "use": "@vercel/node" }],
  "routes": [
    { "src": "/login/(.*)", "dest": "/login/:userId" },
    { "src": "/dash", "dest": "/dash" },
    { "src": "/(.*)", "dest": "/" }
  ]
}
